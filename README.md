# Python.Structures.Queue
This rep  contains example of the queue on python

Also contains two usage examples:
1. Circled queue, used should set queue size and iterations count. Elements from the queue will be dequeued and then enqueued again in it;
2. Queue release based on two stacks;
